[gd_scene load_steps=3 format=2]

[ext_resource path="res://Common/BaseDemo3D.tscn" type="PackedScene" id=1]
[ext_resource path="res://Demos/Outline/Outline3D/Outline3DScene.tscn" type="PackedScene" id=2]

[node name="Outline3DDemo" instance=ExtResource( 1 )]

[node name="Outline3D" parent="DemoView/Viewport" index="0" instance=ExtResource( 2 )]

[node name="RichTextLabel" parent="DemoInterface/MarginContainer" index="0"]
bbcode_text = "Uses shaders to draw an outline around 3D object, effectively drawing them a second time, but bigger, and inverting the culling so it draws behind the object."
text = "Uses shaders to draw an outline around 3D object, effectively drawing them a second time, but bigger, and inverting the culling so it draws behind the object."

[editable path="DemoInterface"]
